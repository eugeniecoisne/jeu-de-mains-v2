<% @footer = false %>

Un avis a été signalé par <%= @review.booking.session.workshop.place.user.profile.company %>

Vérifier l'avis dans l'admin : https://www.jeudemains.com/admin/reviews/<%= @review.id %>

Publié le : <%= @review.created_at.strftime("%d/%m/%Y") %>
Par : <%= @review.user.fullname %>
Consommateur_ID : <%= @review.user.id %>
Consommateur_email : <%= @review.user.email %>
Note : <%= @review.rating %> / 5
Message : <%= @review.content %>

Réservation_ID : <%= @review.booking.id %>
N° de réservation : <%= @review.booking.created_at.strftime("%Y%m") %><%= @review.booking.id %>
Atelier : <%= @review.booking.session.workshop.title %>
Session :
<% if @review.booking.session.start_date == @review.booking.session.end_date %>
<%= l(@review.booking.session.start_date, format: '%A %d %b %Y') %> de <%= @review.booking.session.start_time %> à <%= @review.booking.session.end_time %>
<% else %>
<%= l(@review.booking.session.start_date, format: '%d %b %Y') %> à <%= @review.booking.session.start_time %> au <%= l(@review.booking.session.end_date, format: '%d %b %Y') %> à <%= @review.booking.session.end_time %>
<% end %>
Type : <%= @review.booking.session.workshop.visio == true ? "en visioconférence" : "en présentiel" %>
